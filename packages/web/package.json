{
  "name": "@loam/web",
  "version": "0.1.0",
  "description": "Loam local-first Logseq graph reader and static web app",
  "keywords": [
    "logseq",
    "pwa",
    "github-pages",
    "preact"
  ],
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "loam-web": "dist/server.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b && vite build",
    "prepare": "tsc -b && vite build",
    "dev": "vite",
    "test": "vitest run",
    "lint": "biome check --write src test",
    "lint:ci": "biome check src test",
    "format": "biome format --write src test",
    "format:check": "biome format src test"
  },
  "dependencies": {
    "@loam/cli": "workspace:*",
    "@loam/core": "workspace:*",
    "express": "^5.2.1",
    "preact": "^10.29.2"
  },
  "devDependencies": {
    "@types/express": "latest",
    "@types/node": "latest",
    "@preact/preset-vite": "^2.10.5",
    "cpy-cli": "latest",
    "vitest": "latest",
    "vite": "^8.0.3"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cmwen/logseq-pwa.git",
    "directory": "packages/web"
  },
  "license": "MIT"
}
